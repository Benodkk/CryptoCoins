import { StyledFetchError } from "./FetchError.styled";

const FetchError = () => {
  return (
    <StyledFetchError>
      Cannot fetch the data. Please, try later.
    </StyledFetchError>
  );
};

export default FetchError;
